# 아키텍처 개요

## 한 줄 요약
앱의 큰 구조를 한 번에 이해하기 위한 문서입니다.

## 쉽게 설명하면
- 화면(프론트엔드), 서버(백엔드), 데이터 저장소(DB)가 어떻게 연결되는지 설명합니다.

## 현재 결정사항
- 웹 앱 개발 예정
- 프론트엔드와 백엔드를 분리해서 운영한다.
- 프론트엔드는 모바일 우선 반응형 웹(옵션 A)으로 진행한다.
- 세부 기술 선택은 이후 단계에서 확정한다.

## 다음에 사용자에게 물어볼 질문
- 모바일에서 가장 자주 쓰는 화면은 무엇인가요? (체크리스트/캘린더/독려 피드백)
